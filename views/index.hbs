<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WorkWise Home</title>
    <link rel="stylesheet" href="/style.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script> <!-- SweetAlert library -->
    <style>
        @keyframes borderAnimation {
            0% {
                border-color: #ff00ff;
            }
            50% {
                border-color: #00ffff;
            }
            100% {
                border-color: #ff00ff;
            }
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: url('m.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #FFFFFF;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            overflow-x: hidden;
        }
        /* Styles for big screens */
@media (min-width: 1024px) {
    header {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        padding: 20px;
        background: rgba(0, 0, 0, 0.5); /* Glass effect background */
        backdrop-filter: blur(10px);
        display: flex;
        justify-content: center; /* Center content */
        align-items: center;
        z-index: 1000;
        border: 4px solid;
        border-image-source: linear-gradient(270deg, #00ffff, #ff00ff);
        border-image-slice: 1;
    }

    .nav-list-desktop {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .nav-list-desktop li {
        margin-left: 25px;
    }

    .nav-list-desktop a {
        color: #FFFFFF;
        text-decoration: none;
        font-size: 18px;
        font-weight: bold; /* Thicker font */
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .nav-list-desktop a:hover {
        color: #FFFFFF; /* Text color remains white */
        background: linear-gradient(270deg, #00ffff, #ff00ff); /* Hover effect */
        padding: 5px;
        border-radius: 5px;
    }
}

        header {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px;
            background: #00000080; /* Semi-transparent background */
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
        }

        .nav-list-desktop {
            display: flex;
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .nav-list-desktop li {
            margin-left: 25px;
        }

        .nav-list-desktop a {
            color: #FFFFFF;
            text-decoration: none;
            font-size: 18px;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .nav-list-desktop a:hover {
            color: #000000;
            padding: 5px;
            border-radius: 5px;
        }

        .menu-icon {
            display: none;
            background: linear-gradient(270deg, #00ffff, #ff00ff);
            border: none;
            color: #FFFFFF;
            font-size: 30px;
            cursor: pointer;
            border-radius: 10px;
            padding: 10px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .menu-icon:hover {
            background: linear-gradient(270deg, #00ffff, #ff00ff);
        }

        .menu-container {
            position: relative;
        }

        #menu-btn {
            display: none;
            background: linear-gradient(270deg, #ff00ff, #00ffff);
            color: #FFFFFF;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 10px;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        #menu-btn:hover {
            background: linear-gradient(270deg, #00ffff, #ff00ff);
        }

        .nav-list {
            list-style: none;
            margin: 0;
            padding: 0;
            display: none;
            flex-direction: column;
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1000;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
            padding-top: 60px;
        }

        .nav-list.show {
            display: flex;
        }

        .nav-list li {
            padding: 15px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.3);
        }

        .nav-list li:last-child {
            border-bottom: none;
        }

        .nav-list a {
            color: #FFFFFF;
            text-decoration: none;
            font-size: 20px;
            display: block;
            padding: 10px 0;
            transition: color 0.3s ease;
        }

        .nav-list a:hover {
            color: #ff00ff;
        }

        .hero {
            display: flex;
            justify-content: center;
            align-items: center;
            height: calc(100vh - 60px);
            background: rgba(0, 0, 0, 0.6);
            text-align: center;
            padding: 20px;
            width: 100%;
        }

        .hero-container {
            display: flex;
            justify-content: center;
            align-items: center;
            max-width: 1200px;
            margin: auto;
            padding: 20px;
        }

        .hero-image {
            position: relative;
            max-width: 100%;
            height: auto;
            backdrop-filter: blur(15px);
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
            padding: 20px;
            border: 4px solid;
            border-image-slice: 1;
            border-image-source: linear-gradient(270deg, #ff00ff, #00ffff);
        }

        .hero-content {
            color: #FFFFFF;
            max-width: 600px;
            position: relative;
            z-index: 1;
        }

        .hero-content h2 {
            font-size: 4em;
            margin-bottom: 20px;
            color: #ff00ff;
        }

        .hero-content p {
            font-size: 1.5em;
        }

        .create-account-btn {
            background: linear-gradient(270deg, #ff00ff, #00ffff);
            color: #FFFFFF;
            border: none;
            padding: 15px 30px;
            cursor: pointer;
            border-radius: 10px;
            font-size: 18px;
            transition: background-color 0.3s ease;
            text-transform: uppercase;
            margin-top: 20px;
        }

        .create-account-btn a {
            color: #FFFFFF;
            text-decoration: none;
        }

        .create-account-btn:hover {
            background: linear-gradient(270deg, #00ffff, #ff00ff);
        }

        .modal {
            display: flex;
            align-items: center;
            justify-content: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 999;
        }

        .modal-content {
            backdrop-filter: blur(15px);
            background-color: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 20px;
            position: relative;
            border: 4px solid;
            border-image-slice: 1;
            border-image-source: linear-gradient(270deg, #ff00ff, #00ffff);
            animation: borderAnimation 5s linear infinite;
            color: #FFFFFF;
            width: 350px;
            max-width: 90%;
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
        }

        .close {
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 24px;
            cursor: pointer;
            color: #FFFFFF;
            background-color: rgba(255, 0, 0, 0.5);
            border: none;
            border-radius: 50%;
            padding: 5px 10px;
        }

        button {
            background: linear-gradient(270deg, #ff00ff, #00ffff);
            color: #FFFFFF;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 10px;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background: linear-gradient(270deg, #00ffff, #ff00ff);
        }

        @media (max-width: 768px) {
            header {
                padding: 20px;
            }

            .nav-list-desktop {
                display: none;
            }

            .menu-icon {
                display: block;
                background: linear-gradient(270deg, #00ffff, #ff00ff);
                color: #FFFFFF;
                border: none;
                font-size: 30px;
                cursor: pointer;
                border-radius: 10px;
                padding: 10px;
                transition: background-color 0.3s ease, color 0.3s ease;
            }

            .menu-icon:hover {
                background: linear-gradient(270deg, #00ffff, #ff00ff);
            }

            .hero {
                height: auto;
                margin-top: 0;
            }

            .hero-content h2 {
                font-size: 2.5em;
            }

            .hero-content p {
                font-size: 1em;
            }

            .nav-list .close-link {
                padding: 15px;
                text-align: center;
                border-top: 1px solid rgba(255, 255, 255, 0.3);
                cursor: pointer;
                font-size: 20px;
            }
        }

        @media (min-width: 769px) {
            .nav-list-desktop {
                display: flex;
            }

            .menu-icon {
                display: none;
            }

            .nav-list {
                display: none;
            }

            #menu-btn {
                display: none;
            }
        }
    </style>
</head>
<body>
    <header>
       
        <ul class="nav-list-desktop">
             
            <button id="menu-btn">Menu</button>
        </div>
            <li><a href="/register">Create An Account</a></li>
            <li><a href="/login">Login</a></li>
            <li><a href="/admin-login">Admin Login</a></li>
        </ul>
        <button id="menu-btn">Menu</button>
        <div id="nav-list" class="nav-list">
            <ul>
                <li><a href="/register">Create An Account</a></li>
                <li><a href="/login">Login</a></li>
                <li><a href="/admin-login">Admin Login</a></li>
                <li class="close-link">Close</li>
            </ul>
        </div>
        <button class="menu-icon">&#9776;</button>
    </header>

    <div class="hero">
        <div class="hero-container">
            <div class="hero-image">
                <div class="hero-content">
                    <h2>Welcome to WorkWise</h2>
                    <p>Your gateway to seamless work management</p>
                    <button class="create-account-btn"><a href="/register">Create an Account</a></button>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.querySelector('.menu-icon').addEventListener('click', function() {
            document.getElementById('nav-list').classList.toggle('show');
        });

        document.querySelector('.close-link').addEventListener('click', function() {
            document.getElementById('nav-list').classList.remove('show');
        });
    </script>
</body>
</html>
