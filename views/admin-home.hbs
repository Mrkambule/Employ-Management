<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Management Dashboard</title>
    <link rel="stylesheet" href="dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
        }

        body {
            display: flex;
            height: 100vh;
            background-color: #e0f7fa;
        }

        .container {
            display: flex;
            width: 100%;
        }

        .sidebar {
            width: 250px;
            background: #001f3f;
            color: #ffffff;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            position: fixed;
            top: 0;
            bottom: 0;
            left: 0;
        }

        .sidebar-header {
            padding: 20px;
            text-align: center;
            font-size: 1.5em;
            border-bottom: 1px solid rgba(255, 255, 255, 0.3);
        }

        .sidebar-nav {
            flex-grow: 1;
        }

        .sidebar-nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .sidebar-nav ul li {
            padding: 15px 20px;
        }

        .sidebar-nav ul li a {
            color: #ffffff;
            text-decoration: none;
            display: flex;
            align-items: center;
            transition: background 0.3s;
        }

        .sidebar-nav ul li a:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }

        .sidebar-nav ul li a i {
            margin-right: 10px;
        }

        .main-content {
            flex: 1;
            margin-left: 250px;
            display: flex;
            flex-direction: column;
            background-color: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            overflow-y: auto;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: #001f3f;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            color: #ffffff;
        }

        .header-actions {
            display: flex;
            align-items: center;
        }

        .logout-btn {
            padding: 10px 20px;
            background: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .logout-btn:hover {
            background: #0056b3;
        }

        .content {
            padding: 20px;
        }

        .cards {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .card {
            flex: 1;
            background-color: #007bff;
            backdrop-filter: blur(10px);
            padding: 20px;
            margin: 0 10px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            color: #ffffff;
        }

        .card h3 {
            margin-bottom: 15px;
            color: #ffffff;
        }

        .card p {
            font-size: 1.5em;
            color: #ffffff;
        }

        .charts {
            display: flex;
            justify-content: space-between;
        }

        .chart {
            flex: 1;
            background-color:  #007bff;
            backdrop-filter: blur(10px);
            padding: 20px;
            margin: 0 10px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            color: #ffffff;
        }

        .chart h3 {
            margin-bottom: 15px;
            color: #ffffff;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .cards, .charts {
                flex-direction: column;
            }

            .card, .chart {
                margin: 10px 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2>WorkWise</h2>
            </div>
            <nav class="sidebar-nav">
                <ul>
                    <li><a href="#"><i class="fas fa-home"></i> Dashboard</a></li>
                    <li><a href="#"><i class="fas fa-user"></i> Employees</a></li>
                    <li><a href="/register-employee"><i class="fas fa-user-plus"></i> Add Employee</a></li>
                    <li><a href="#"><i class="fas fa-tasks"></i> Create Task</a></li>
                    <li><a href="#"><i class="fas fa-check-circle"></i> Completed Tasks</a></li>
                    <li><a href="#"><i class="fas fa-hourglass-half"></i> Pending Tasks</a></li>
                    <li><a href="#"><i class="fas fa-file-alt"></i> Reports</a></li>
                    <li><a href="#"><i class="fas fa-cogs"></i> Settings</a></li>
                </ul>
            </nav>
        </aside>
        <main class="main-content">
            <header>
                <h1>Dashboard</h1>
                <div class="header-actions">
                    <button class="logout-btn">Logout</button>
                </div>
            </header>
            <section class="content">
                <div class="cards">
                    <div class="card">
                        <h3>Total Employees</h3>
                        <p>150</p>
                    </div>
                    <div class="card">
                        <h3>Departments</h3>
                        <p>5</p>
                    </div>
                    <div class="card">
                        <h3>New Hires</h3>
                        <p>10</p>
                    </div>
                </div>
                <div class="charts">
                    <div class="chart">
                        <h3>Employee Distribution</h3>
                        <!-- Placeholder for a chart -->
                    </div>
                    <div class="chart">
                        <h3>Monthly Hires</h3>
                        <!-- Placeholder for a chart -->
                    </div>
                </div>
            </section>
        </main>
    </div>
</body>
</html>
